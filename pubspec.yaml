name: eduverse # Make sure this matches the 'eduverse' in your imports
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Database and Routing
  supabase_flutter: ^2.8.3
  go_router: ^14.2.0

  # Required for Teacher app components
  google_fonts: ^6.1.0
  flutter_animate: ^4.5.0
  lucide_icons_flutter: ^1.0.0
  file_picker: ^8.0.0
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true